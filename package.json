{
  "name": "s0-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.18.3"
}
